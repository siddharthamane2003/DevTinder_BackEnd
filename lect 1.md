.env => We used for th security purpsoe
- In that all scuriuty will be stored like connection string , port , jswpass
- process.env is an object in Node.js that stores environment variables.
- why env not in fonrt bause of the brower env that aee confiure on th srvser and also the all procss.env.
- In front we can used bause these is in client side configration and
 “In frontend, .env variables are bundled into the client code, so they’re not secure and can be viewed in the browser.”
- for that we have to instal npm i dotenv and imprt with config.
- In front end either use the loachowt or the "/" url
- But thes not the production  in we have to the env in gitingotre